export interface IESIAsset {
  is_blueprint_copy?: string,
  is_singleton: boolean,
  item_id: number,
  location_flag: string,
  location_id: number,
  location_type: string,
  quantity: number,
  type_id: number

}